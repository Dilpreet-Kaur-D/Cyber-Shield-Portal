from main_login import LoginApp#Start the app with the login screen
if __name__=="__main__":
    app=LoginApp()#creating an object of the LoginApp class
    app.mainloop()#Keep the window open, alive, and responsive until the user closes it.
 